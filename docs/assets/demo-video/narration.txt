在真实世界里，一条生存曲线背后，是医生的选择、患者的等待、团队的解释。我们把这些证据带进平台，让建模从一开始就可追溯。

上传 KM、生存表或风险表，系统自动对齐时间、校验字段，生成可复用的证据对象。

证据被编译成可调用的概率函数，Markov 和 PSA 不再靠手工换算，每一个参数都有来源。

临床校准把模型拉回真实世界，观察值和预测值在同一张图上对齐，偏差一目了然。

运行模拟后，队列在状态间动态流动，成本、QALY 与不确定性一起呈现。

最后，这些结果被整理成可审阅、可解释、可交付的报告。让决策更快，也更让人信服。
